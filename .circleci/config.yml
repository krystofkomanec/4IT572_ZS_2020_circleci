version: 2.1
orbs:
  node: circleci/node@3.0.0
vse: &vse
docker:
  - image: circleci/node:8.11.3

version: 2.1
jobs:
  build:
    <<: *vse
    steps:
      - checkout
      - run: npm install
      - run: npm run test
      - run: npm build
workflows:
  node-tests:
    jobs:
      - node/test
